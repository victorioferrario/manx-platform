export enum LoadStateEnum {
    None  = 100,
    Loaded = 200,
    Loading = 300,    
    UnLoaded = 400,
    UnLoading = 500
}
export enum VisibleEnum {
    None = 0,
    Show = 100,
    Hide = 200
}

// { LoadEnum, LoadDisplayEnum }