export type Size = SizeEnum.small | SizeEnum.medium | SizeEnum.large;
export enum SizeEnum {
    small = "small",
    medium = "medium",
    large = "large"
}
export type Mode = ModeEnum.side | ModeEnum.push | ModeEnum.over;
export enum ModeEnum {
    side = "side",
    push = "push",
    over = "over"
}
